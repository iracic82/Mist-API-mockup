services:
  data-viewer:
    build: .
    ports:
      - "8080:80"
    environment:
      - API_URL=${API_URL:-https://mist-api-controller.highvelocitynetworking.com}
      - API_KEY=${API_KEY:-}
    restart: unless-stopped
